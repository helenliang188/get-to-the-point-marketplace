interface:
  display_name: "Get To The Point"
  short_description: "Rewrite comms with clear, evidence-led language"
  default_prompt: "Use $get-to-the-point to rewrite this paragraph so it gets to the point, sounds natural, uses evidence, and explains key decisions through values."
